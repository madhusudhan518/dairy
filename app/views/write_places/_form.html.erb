<%= form_for(@write_place) do |f| %>
  <% if @write_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@write_place.errors.count, "error") %> prohibited this write_place from being saved:</h2>

      <ul>
      <% @write_place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
